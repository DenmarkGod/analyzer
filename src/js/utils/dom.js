export const $ = (selector, context = document) =; export const $$ = (selector, context = document) =;  export const createElement = (tagName, props = {}, children = []) =  const el = document.createElement(tagName);   Object.assign(el, props);   children.forEach(child =    if (typeof child === 'string') el.textContent += child;     else el.appendChild(child);   });   return el; }; 
